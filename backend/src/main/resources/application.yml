server:
  port: ${PORT:8080}

spring:
  application:
    name: backend
  flyway:
    enabled: true

google:
  client-id: ${GOOGLE_CLIENT_ID}

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: when-authorized
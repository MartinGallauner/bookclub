server:
  port: ${PORT:8080}

spring:
  application:
    name: backend
  datasource:
    url:
    username: dev
    password: dev
  flyway:
    enabled: true

google:
  client-id: ${GOOGLE_CLIENT_ID}

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: when-authorized